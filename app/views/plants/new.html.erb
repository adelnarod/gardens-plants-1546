<h1>Add Plant to <%= @garden.name %></h1>

<div class="container">
  <%= simple_form_for [@garden, @plant] do |f| %>
    <%= f.input :name %>
    <%= f.input :photo_url %>
    <%= f.submit %>
  <% end %>
</div>
